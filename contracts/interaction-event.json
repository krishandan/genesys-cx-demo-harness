{
  "name": "Backlot - Record Interaction",
  "integrationType": "custom-rest-actions",
  "actionType": "custom",
  "secure": false,
  "config": {
    "request": {
      "requestUrlTemplate": "https://backlot-api.krishharness.com/gx/interaction-event",
      "requestType": "POST",
      "headers": {
        "X-API-Key": "${credentials.apiKey}",
        "Content-Type": "application/json"
      },
      "requestTemplate": "{\"identifier\": \"$esc.jsonString(\"${input.identifier}\")\",\"channel\": \"$esc.jsonString(\"${input.channel}\")\",\"kind\": \"$esc.jsonString(\"${input.kind}\")\"}"
    },
    "response": {
      "translationMap": {},
      "translationMapDefaults": {},
      "successTemplate": "${rawResult}"
    }
  },
  "contract": {
    "input": {
      "inputSchema": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "title": "Backlot - Record Interaction input",
        "type": "object",
        "properties": {
          "identifier": {
            "type": "string",
            "description": "Raw identifier: ANI/phone (E.164 or national), email, or account number. Normalized server-side, so an unencoded '+' is tolerated."
          },
          "channel": {
            "type": "string",
            "description": "webmessaging | voice | sms | email | ..."
          },
          "kind": {
            "type": "string",
            "description": "inbound | outbound"
          }
        },
        "required": [
          "channel",
          "identifier"
        ],
        "additionalProperties": false
      }
    },
    "output": {
      "successSchema": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "title": "InteractionEventOut output",
        "type": "object",
        "properties": {
          "ok": {
            "type": "boolean",
            "description": "True when the interaction was recorded."
          },
          "party_id": {
            "type": "string",
            "description": "Internal subscriber id. Never read it aloud."
          },
          "stored": {
            "type": "boolean",
            "description": "True when the event was persisted."
          },
          "last_channel": {
            "type": "string",
            "description": "The channel customer-context will now report for this subscriber."
          }
        },
        "required": [
          "last_channel",
          "ok",
          "party_id",
          "stored"
        ],
        "additionalProperties": true
      }
    }
  }
}
