{
  "name": "Backlot - Network Status",
  "integrationType": "custom-rest-actions",
  "actionType": "custom",
  "secure": false,
  "config": {
    "request": {
      "requestUrlTemplate": "https://backlot-api.krishharness.com/gx/net-status?identifier=$esc.url(\"${input.identifier}\")",
      "requestType": "GET",
      "headers": {
        "X-API-Key": "${credentials.apiKey}"
      },
      "requestTemplate": "${input.rawRequest}"
    },
    "response": {
      "translationMap": {},
      "translationMapDefaults": {},
      "successTemplate": "${rawResult}"
    }
  },
  "contract": {
    "input": {
      "inputSchema": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "title": "Backlot - Network Status input",
        "type": "object",
        "properties": {
          "identifier": {
            "type": "string",
            "description": "Raw identifier: ANI/phone (E.164 or national), email, or account number. Normalized server-side, so an unencoded '+' is tolerated."
          }
        },
        "required": [
          "identifier"
        ],
        "additionalProperties": false
      }
    },
    "output": {
      "successSchema": {
        "$schema": "http://json-schema.org/draft-04/schema#",
        "title": "NetStatusOut output",
        "type": "object",
        "properties": {
          "found": {
            "type": "boolean",
            "description": "False when the identifier resolves to no subscriber."
          },
          "party_id": {
            "type": "string",
            "description": "Internal subscriber id. Never read it aloud."
          },
          "healthy": {
            "type": "boolean",
            "description": "True when no fault is detected. Use this to confirm the fix worked."
          },
          "fault_type": {
            "type": "string",
            "description": "The remaining fault, or none when healthy."
          },
          "wan_ok": {
            "type": "boolean",
            "description": "True when the broadband line into the home is healthy."
          },
          "wan_status": {
            "type": "string",
            "description": "Line state: online, degraded or offline."
          },
          "gateway_model": {
            "type": "string",
            "description": "Model name of the customer's router/hub."
          },
          "gateway_uptime_s": {
            "type": "integer",
            "description": "Seconds since the router last restarted."
          },
          "ap_total": {
            "type": "integer",
            "description": "How many access points (hub plus boosters) are in the home."
          },
          "ap_online": {
            "type": "integer",
            "description": "How many of those are currently online."
          },
          "extender_status": {
            "type": "string",
            "description": "Health of the booster: online, flapping, offline, or none."
          },
          "device_total": {
            "type": "integer",
            "description": "How many devices are connected in the home."
          },
          "devices_on_target_band": {
            "type": "integer",
            "description": "How many devices are on the faster 5GHz band."
          },
          "worst_device_label": {
            "type": "string",
            "description": "Human name of the device with the weakest signal."
          },
          "worst_device_band": {
            "type": "string",
            "description": "Band of the weakest device: 2.4 or 5."
          },
          "worst_device_rssi": {
            "type": "integer",
            "description": "Signal of the weakest device in dBm; below -70 is poor."
          },
          "coverage": {
            "type": "string",
            "description": "Whether the home has a structural coverage weakness: good or weak. This is SEPARATE from fault_type \u2014 a home can have fault_type none and still have weak coverage. A reboot or band-steer does NOT fix weak coverage; only additional equipment (a mesh point) does. Use this to recommend an upgrade as a fact, never as a fix for the current fault."
          },
          "coverage_note": {
            "type": "string",
            "description": "Plain-English, speakable summary of the coverage weakness, e.g. \"Two devices are hanging at the edge of the Upstairs Extender's range.\" Empty when coverage is good. Say this to explain why a mesh upgrade would help."
          },
          "coverage_device_count": {
            "type": "integer",
            "description": "How many devices are sitting at the edge of range. 0 when coverage is good. This does not change when a fault is fixed \u2014 it is about distance, not faults."
          },
          "coverage_worst_area": {
            "type": "string",
            "description": "Where the weak coverage is, e.g. \"Upstairs Extender\". Empty when coverage is good. Use it to tell the customer which part of the home needs a mesh point."
          }
        },
        "required": [
          "ap_online",
          "ap_total",
          "coverage",
          "coverage_device_count",
          "coverage_note",
          "coverage_worst_area",
          "device_total",
          "devices_on_target_band",
          "extender_status",
          "fault_type",
          "found",
          "gateway_model",
          "gateway_uptime_s",
          "healthy",
          "party_id",
          "wan_ok",
          "wan_status",
          "worst_device_band",
          "worst_device_label",
          "worst_device_rssi"
        ],
        "additionalProperties": true
      }
    }
  }
}
