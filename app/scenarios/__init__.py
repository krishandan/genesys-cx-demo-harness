"""Scenario engine: stage and reset demo state declaratively.

A scenario is a YAML file naming a target state as field-setters over existing
entities (match + set). Adding a scenario is a file, never code.
"""
