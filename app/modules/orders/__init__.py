"""Orders: place an offer and confirm it.

Follows the established hybrid pattern — one action endpoint per module, verbs register
handlers — so a new verb needs neither a new route nor a new Genesys data action.
"""
